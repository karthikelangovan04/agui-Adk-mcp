# Weather App - Complete Integration Flow

## 🎯 High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                    USER BROWSER                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  http://localhost:3000                                    │  │
│  │  Next.js Frontend with CopilotKit                        │  │
│  │  - Chat Interface                                         │  │
│  │  - Weather Cards                                          │  │
│  │  - Alert Cards                                            │  │
│  │  - HITL Confirmation UI                                   │  │
│  └────────────────────┬─────────────────────────────────────┘  │
└─────────────────────────┼───────────────────────────────────────┘
                          │ POST /api/copilotkit
                          │ (SSE Stream)
                          ▼
┌─────────────────────────────────────────────────────────────────┐
│              NEXT.JS API ROUTE (Proxy)                          │
│  /app/api/copilotkit/route.ts                                  │
│  - Extracts nested body from CopilotKit                        │
│  - Adds threadId, runId, etc.                                  │
│  - Proxies to Python backend                                   │
└────────────────────┬────────────────────────────────────────────┘
                     │ POST http://127.0.0.1:8000/
                     │ (SSE Stream)
                     ▼
┌─────────────────────────────────────────────────────────────────┐
│           PYTHON BACKEND (FastAPI)                              │
│  backend_tool_rendering.py                                      │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  ADKAgent (ag-ui-adk middleware)                         │  │
│  │  - Session management                                     │  │
│  │  - SSE event streaming                                    │  │
│  │  - Tool execution                                         │  │
│  └────────────────────┬─────────────────────────────────────┘  │
│                       │                                          │
│  ┌────────────────────▼─────────────────────────────────────┐  │
│  │  Google ADK Agent (name="assistant")                     │  │
│  │  Model: gemini-2.0-flash                                 │  │
│  │  ┌────────────────────────────────────────────────────┐  │  │
│  │  │  Agent Instructions:                                │  │  │
│  │  │  1. Geocode location                               │  │  │
│  │  │  2. Call confirm_weather_query (HITL)              │  │  │
│  │  │  3. Based on user selection:                       │  │  │
│  │  │     - get_forecast for weather                     │  │  │
│  │  │     - get_alerts for alerts                        │  │  │
│  │  │  4. Present results naturally                      │  │  │
│  │  └────────────────────────────────────────────────────┘  │  │
│  │                                                            │  │
│  │  Tools Available:                                          │  │
│  │  ┌─────────────────────────────────────────────────────┐ │  │
│  │  │  McpToolset (weather_toolset)                       │ │  │
│  │  │  - geocode_location(location: str)                  │ │  │
│  │  │  - get_forecast(lat: float, lon: float)            │ │  │
│  │  │  - get_alerts(state: str)                          │ │  │
│  │  └───────────────────┬─────────────────────────────────┘ │  │
│  └────────────────────────┼───────────────────────────────────┘  │
└───────────────────────────┼─────────────────────────────────────┘
                            │ STDIO Communication
                            │ (uv run python weather/weather.py)
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│           MCP WEATHER SERVER                                    │
│  weather/weather.py (FastMCP)                                   │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  @mcp.tool() geocode_location                           │  │
│  │  - Calls OpenStreetMap Nominatim API                    │  │
│  │  - Returns: lat, lon, display_name                      │  │
│  └──────────────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  @mcp.tool() get_forecast                               │  │
│  │  - Calls National Weather Service API                   │  │
│  │  - Returns: temp, conditions, wind, periods             │  │
│  └──────────────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  @mcp.tool() get_alerts                                 │  │
│  │  - Calls NWS Alerts API                                 │  │
│  │  - Returns: alerts array with severity                  │  │
│  └──────────────────────────────────────────────────────────┘  │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│               EXTERNAL APIs                                     │
│  - OpenStreetMap Nominatim (geocoding)                         │
│  - National Weather Service (forecast & alerts)                │
└─────────────────────────────────────────────────────────────────┘
```

## 📝 Detailed Request Flow

### Example: "What's the weather in San Francisco?"

```
┌─────────────────────────────────────────────────────────────────┐
│ STEP 1: User Input                                              │
└─────────────────────────────────────────────────────────────────┘
User types: "What's the weather in San Francisco?"
         ↓
CopilotKit captures message
         ↓
POST /api/copilotkit with:
{
  "messages": [{"role": "user", "content": "What's the weather in San Francisco?"}],
  "threadId": "...",
  "tools": [...],
  ...
}

┌─────────────────────────────────────────────────────────────────┐
│ STEP 2: Backend Agent Processing                               │
└─────────────────────────────────────────────────────────────────┘
ADK Agent receives message
         ↓
Gemini model processes with instructions
         ↓
Decides to call: geocode_location("San Francisco")
         ↓
MCP Tool executed via STDIO
         ↓
Returns: {
  "latitude": 37.7879363,
  "longitude": -122.4075201,
  "display_name": "San Francisco, California, United States"
}

┌─────────────────────────────────────────────────────────────────┐
│ STEP 3: Human-in-the-Loop Confirmation                         │
└─────────────────────────────────────────────────────────────────┘
Agent calls: confirm_weather_query
         ↓
SSE Event: TOOL_CALL_START
         ↓
Frontend renders WeatherQueryConfirmation component:
┌──────────────────────────────────────────────┐
│ 🌤️  Confirm Weather Query                   │
│ San Francisco, California, United States     │
│ 📍 37.7879, -122.4075                        │
│                                              │
│ Select information to retrieve:              │
│ [✓] 🌡️  Get current forecast                │
│ [✓] 🚨  Check weather alerts                 │
│                                              │
│     [✗ Reject]    [✓ Confirm (2)]           │
└──────────────────────────────────────────────┘
         ↓
User clicks "Confirm"
         ↓
Frontend sends: { accepted: true, selectedActions: ["forecast", "alerts"] }
         ↓
SSE Event: TOOL_CALL_RESULT

┌─────────────────────────────────────────────────────────────────┐
│ STEP 4: Fetch Selected Data                                    │
└─────────────────────────────────────────────────────────────────┘
Agent receives confirmation
         ↓
Calls: get_forecast(37.7879, -122.4075)
         ↓
MCP Tool executes:
  1. GET https://api.weather.gov/points/37.7879,-122.4075
  2. GET https://api.weather.gov/gridpoints/MTR/85,106/forecast
         ↓
Returns: {
  "temperature": 12.8,
  "temperature_f": 55,
  "conditions": "clear",
  "windSpeed": 9,
  "windSpeedText": "9 mph",
  "windDirection": "NE",
  "location": "Sausalito, CA"
}
         ↓
SSE Event: TOOL_CALL_END
         ↓
Also calls: get_alerts("CA")
         ↓
Returns: { "alerts": [...], "count": 17 }

┌─────────────────────────────────────────────────────────────────┐
│ STEP 5: Frontend Rendering                                     │
└─────────────────────────────────────────────────────────────────┘
Frontend receives tool results
         ↓
useCopilotAction("get_forecast") renders WeatherCard:
┌──────────────────────────────────────────────┐
│  Sausalito, CA     🌞 [Sun Icon]            │
│  Current Weather                             │
│                                              │
│  13° C  / 55° F           Clear             │
│  ────────────────────────────────────────    │
│   Humidity    Wind         Feels Like       │
│     0%      9 mph NE          13°C          │
└──────────────────────────────────────────────┘
         ↓
useCopilotAction("get_alerts") renders AlertsCard:
┌──────────────────────────────────────────────┐
│ 🚨 Weather Alerts for CA     [17 Active]    │
│                                              │
│ ⚠️  Severe Alerts (3)                       │
│ [Extreme Cold Warning]  [Severe]            │
│ [High Wind Warning]     [Severe]            │
│ ...                                          │
│                                              │
│ ⚡ Moderate Alerts (14)                     │
│ [Cold Weather Advisory] [Moderate]          │
│ ...                                          │
└──────────────────────────────────────────────┘
```

## 🔄 Data Format Transformations

### MCP Tool Output → Frontend Rendering

```javascript
// MCP get_forecast returns:
{
  "temperature": 12.8,        // Celsius
  "temperature_f": 55,         // Fahrenheit
  "conditions": "clear",
  "windSpeed": 9,
  "windSpeedText": "9 mph",
  "windDirection": "NE",
  "feelsLike": 12.8,
  "location": "Sausalito, CA",
  "humidity": 0,
  "periods": [...]
}

// Frontend receives directly (no transformation needed!)
// Passes to WeatherCard component props:
<WeatherCard
  location="Sausalito, CA"
  temperature={12.8}
  temperature_f={55}
  conditions="clear"
  windSpeed={9}
  windSpeedText="9 mph"
  windDirection="NE"
  feelsLike={12.8}
  humidity={0}
  themeColor="#667eea"  // Calculated from conditions
  status="complete"
/>
```

## 🎨 UI Components Hierarchy

```
AgenticChat (Root)
  └─ CopilotKit (Provider)
      └─ Chat (Main Component)
          ├─ useHumanInTheLoop("confirm_weather_query")
          │   └─ WeatherQueryConfirmation
          │       ├─ Location display
          │       ├─ Coordinates display  
          │       ├─ Options checkboxes
          │       └─ Confirm/Reject buttons
          │
          ├─ useCopilotAction("get_forecast")
          │   └─ WeatherCard
          │       ├─ WeatherIcon (Sun/Rain/Cloud)
          │       ├─ Temperature (C/F)
          │       ├─ Conditions
          │       └─ Details (Humidity/Wind/Feels Like)
          │
          ├─ useCopilotAction("get_alerts")
          │   └─ AlertsCard
          │       ├─ Severe Alerts Section
          │       │   └─ AlertItem (expandable)
          │       ├─ Moderate Alerts Section
          │       │   └─ AlertItem (expandable)
          │       └─ Other Alerts Section
          │           └─ AlertItem (expandable)
          │
          └─ CopilotChat (Chat Interface)
              ├─ Initial message
              └─ Suggestions
```

## 🚀 Key Features

### Backend
- ✅ ADK Agent with Gemini 2.0 Flash
- ✅ MCP Toolset integration (stdio connection)
- ✅ Human-in-the-loop confirmation tool
- ✅ Clear workflow instructions
- ✅ Real-time SSE streaming
- ✅ Session management

### Frontend
- ✅ CopilotKit integration
- ✅ Beautiful gradient UI components
- ✅ Interactive HITL confirmation
- ✅ Dynamic weather card theming
- ✅ Expandable alert details
- ✅ Severity-based alert grouping
- ✅ Responsive design
- ✅ TypeScript type safety

### MCP Tools
- ✅ Geocoding via OpenStreetMap
- ✅ Weather forecasts via NWS
- ✅ Weather alerts via NWS
- ✅ Proper error handling
- ✅ JSON response formatting

## 📊 Current Status

**FULLY OPERATIONAL** ✅

All components tested and working:
- ✅ Backend server running (port 8000)
- ✅ Frontend server running (port 3000)
- ✅ MCP tools tested with real data
- ✅ UI components implemented
- ✅ HITL flow configured
- ✅ Agent instructions optimized

Ready for user testing! 🎉

